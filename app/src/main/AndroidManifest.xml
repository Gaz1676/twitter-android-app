<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="app.tweeting">

    <application
        android:name="app.tweeting.main.TweetApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="TweetApp"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name="app.tweeting.activity.Welcome"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="app.tweeting.activity.Tweet"
            android:label="@string/app_name" />
        <activity
            android:name="app.tweeting.activity.Signup"
            android:label="@string/app_name" />
        <activity
            android:name="app.tweeting.activity.Login"
            android:label="@string/app_name" />
    </application>

</manifest>